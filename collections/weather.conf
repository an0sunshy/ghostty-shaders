# Collection manifest for shaders/weather/.
# Read by `ghostty-shaders list` (description) and documents the strategy.
description = Live weather-driven scenes, auto-selected by a 5-min poller
strategy = poller
