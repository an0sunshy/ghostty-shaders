# Discussions are intentionally not enabled on this repo; questions are
# welcome as plain (blank) issues, while bugs, feature requests, and new
# scenes have structured templates.
blank_issues_enabled: true
